
/*
 
Write a program that: 
  - main thread creates another therad. - pthread_create 
  - in the manin thread, it should print out ht e new thread's id, wait until it terminates and print
  out the return value - pthread, phtread_join
  - the other thread should get a string from the main thread as the argument, print that out as
  well as its own ID, and reutrn the length of the received string.
      - stream as the main argument

functions to use: 

- pthread_create
- pthread_join

  */


